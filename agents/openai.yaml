display_name: 开发工作流
short_description: 用 harness、ACTIVE 隔离和 loop 工具，把开发任务转成可接力、可验证、可审核的闭环。
default_prompt: /dev-workflow init；后续描述 Bug、功能、PRD 或改版任务，先跑 harness，用 active-work 锁定 ACTIVE，再用 loop-work 小步验证。
